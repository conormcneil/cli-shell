#!/usr/bin/env node

console.log("hello, world!");
console.log("=============");

// Define dependencies/modules
var shell = require("shelljs");
var colors = require("colors");

// Clear terminal window for improved view
shell.exec("clear");

shell.cd("/");

// Begin script
console.log("hi! this is red".red);